language: ruby
script:
 - docker build --tag steinhoem/nbicreferences -f "Nbic.References/Dockerfile" . 
deploy:
  skip_cleanup: true
  provider: script
  script: bash ./deploy.sh
  on:
    branch: master
